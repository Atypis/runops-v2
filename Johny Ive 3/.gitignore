node_modules/
.next/
.env.local
.env

# Python (keep minimal - this is a TypeScript project)
__pycache__/
*.py[cod]
*.so

# Build artifacts (appropriate for TypeScript/Next.js)
dist/
*.egg-info/

# Note: Removed Python-specific patterns that don't apply to TypeScript:
# - lib/ (TypeScript source code!)
# - lib64/ (Python-specific)
# - parts/, sdist/, var/, wheels/, eggs/, .eggs/ (Python packaging)
# - build/ (could conflict with TypeScript build dirs)
# - develop-eggs/, .installed.cfg, *.egg (Python packaging)

# Virtual environments
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs (runtime logs, not source code)
*.log
/logs/
app_frontend/browser-integration.log
app_frontend/websocket.log
app_frontend/ws-server.log
test-harness/operator/backend/backend.log
test-harness/operator/server.log
ws-server.log

# Screenshots (keep structure but ignore large files)
sop_orchestrator/screenshots/*/screenshot_*.png
sop_orchestrator/screenshots/*/timeline_export_*.json

# Temporary files
*.tmp
*.temp
app_frontend/tsconfig.tsbuildinfo
test-harness/operator/temp/screenshots/

# Browser profiles for session persistence
test-harness/operator/browser-profiles/
browser-profiles/
